# Web Framework & Server
fastapi==0.110.1
uvicorn[standard]==0.29.0

# Database & Authentication
firebase-admin==6.5.0

# Data Validation & Environment Configuration
pydantic==2.6.4
pydantic-settings==2.2.1

# QR Code Generation & Image Processing
qrcode[pil]==7.4.2
Pillow

# Email — HTTP-based API (replaces SMTP which is blocked on Render free tier)
# Render blocks outbound connections on port 587, causing "Network is unreachable".
# Resend uses HTTPS (port 443) which is always open.
resend==2.0.0

# Input validation
email-validator==2.1.1