# Web Framework & Server
fastapi==0.110.1
uvicorn[standard]==0.29.0

# Database & Authentication
firebase-admin==6.5.0

# Data Validation & Environment Configuration
pydantic==2.6.4
pydantic-settings==2.2.1

# QR Code Generation & Image Processing
qrcode[pil]==7.4.2
Pillow

# Email — SendGrid HTTP API
# Render blocks outbound SMTP (port 587). SendGrid uses HTTPS (port 443).
# Free tier: 100 emails/day. No custom domain needed — just Single Sender Verification.
sendgrid==6.11.0

# Input validation
email-validator==2.1.1